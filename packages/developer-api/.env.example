# Copy to .env
ISSUER_URL=http://localhost:3001
WALLET_URL=http://localhost:3000
# Where wallets post proofs back, as reachable from a customer's phone.
# PUBLIC_API_URL=https://<tunnel>/api/dev
PORT=3006
